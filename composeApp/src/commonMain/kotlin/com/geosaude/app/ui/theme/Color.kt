package com.geosaude.app.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta GeoSaude (baseada no Figma)
object GeoSaudeColors {
    // Verdes principais (do Figma)
    val Background = Color(0xFFC9E4D8)  // Verde menta claro (fundo)
    val Primary = Color(0xFF4A9B6F)     // Verde médio (GeoSaúde text + botão)
    val PrimaryDark = Color(0xFF2E7D55) // Verde escuro (hover)

    // Neutros
    val White = Color(0xFFFFFFFF)
    val CardBackground = Color(0xFFFDFDFD) // Branco levemente off-white
    val Gray100 = Color(0xFFF3F4F6)
    val Gray300 = Color(0xFFD1D5DB)
    val Gray400 = Color(0xFF9CA3AF)
    val Gray600 = Color(0xFF6B7280)
    val TextPrimary = Color(0xFF000000)
    val TextSecondary = Color(0xFF6B7280)
    val InputBorder = Color(0xFFE5E7EB)

    // Estados
    val Error = Color(0xFFEF4444)
    val Success = Color(0xFF10B981)
}